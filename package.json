{
  "dependencies": {
    "discord-api-types": "^0.33.0",
    "itty-router": "^2.6.1"
  },
  "scripts": {
    "build": "esbuild --bundle --minify --outfile=./dist/out.js ./src/index.ts",
    "deploy": "wrangler publish dist/out.js"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^3.10.0",
    "dotenv": "^16.0.1",
    "esbuild": "^0.14.39",
    "miniflare": "^2.4.0",
    "node-fetch": "^3.2.4",
    "wrangler": "^2.0.5"
  }
}
