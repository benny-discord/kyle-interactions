{
  "dependencies": {
    "discord-api-types": "^0.27.3",
    "esbuild": "^0.14.24",
    "wrangler": "^0.0.17"
  },
  "scripts": {
    "build": "esbuild --bundle --minify --outfile=./out.js ./src/index.ts",
    "deploy": "wrangler publish out.js"
  },
  "browser": {},
  "devDependencies": {
    "@cloudflare/workers-types": "^3.4.0",
    "webpack-cli": "^4.9.2"
  }
}
