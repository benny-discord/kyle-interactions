{
  "dependencies": {
    "discord-api-types": "^0.26.0",
    "esbuild": "^0.14.11",
    "wrangler": "^0.0.6"
  },
  "scripts": {
    "build": "esbuild --bundle --minify --outfile=./out.js ./src/index.js",
    "deploy": "wrangler publish out.js"
  },
  "browser": {},
  "devDependencies": {
    "webpack-cli": "^4.9.1"
  }
}
