{
  "dependencies": {
    "discord-api-types": "^0.34.0",
    "itty-router": "^2.6.1"
  },
  "scripts": {
    "publish": "wrangler publish ./src/index.ts --format=modules",
    "test": "wrangler publish ./src/index.ts --format=modules --dry-run"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^3.13.0",
    "dotenv": "^16.0.1",
    "node-fetch": "^3.2.6",
    "wrangler": "^2.0.14"
  }
}
