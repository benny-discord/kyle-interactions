{
  "dependencies": {
    "discord-api-types": "^0.33.0",
    "itty-router": "^2.6.1"
  },
  "scripts": {
    "deploy": "wrangler publish ./src/index.ts --format=modules",
    "test": "wrangler publish ./src/index.ts --format=modules --dry-run"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^3.10.0",
    "dotenv": "^16.0.1",
    "node-fetch": "^3.2.4",
    "wrangler": "^2.0.5"
  }
}
